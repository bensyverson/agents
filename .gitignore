# Secrets / local config
.env

# macOS
.DS_Store

# Go build artifacts
*.exe
*.test
*.out
/bin/
coverage.*
# `go build ./...` emits the cmd binary to the repo root

# SQLite databases (server data store + local job state) and WAL sidecars
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

local/

public/
.hugo_build.lock

# Claude Code agent worktrees (transient, machine-local)
.claude/worktrees/

# timetattle local time-tracking marker (machine-generated, per-clone)
.timetattle-project.json

